name: Continuos integration and deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  DOTNET_VERSION: '3.1.x'
  AZURE_WEBAPP_NAME: contact-book

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code ✔
        uses: actions/checkout@v2
    
      - name: Setup .NET Core 3.1.x 🛠️
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Start SQL Server
        run: |
          docker run -d -e "ACCEPT_EULA=Y" -e "qwerty123" -e "MSSQL_PID=Express" -p 1433:1433 -d mcr.microsoft.com/mssql/server:latest 

      - name: Sleep for 10 seconds
        uses: jakejarvis/wait-action@master
        with:
          time: '10s'

      - name: Restore dependencies 📦
        run: dotnet restore

      - name: Build App 🏗️
        run: dotnet build -c Release --no-restore

  # test:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Check out code ✔
  #       uses: actions/checkout@v2
    
  #     - name: Setup .NET Core 3.1.x 🛠️
  #       uses: actions/setup-dotnet@v1
  #       with:
  #         dotnet-version: ${{ env.DOTNET_VERSION }}
        
      - name: Run automated test 🧪
        env:
          ConnectionStrings__DefaultConnection: Server=localhost;Database=AppDataBase;User=sa;Password=qwerty123
        run: dotnet test "$GITHUB_WORKSPACE/Test/Examen.Tests" -c Release --no-build

  # publish:
  #   runs-on: ubuntu-latest
  #   needs: test
  #   steps:
      - name: Publish app 📰
        run: dotnet publish -c Release -o ./out

  # # deploy:
  # #   runs-on: ubuntu-latest
  # #   needs: publish
  # #   steps:
      # - name: Deploy to Azure Web App 🚀
      #   uses: azure/webapps-deploy@v2
      #   with: 
      #     app-name: ${{ env.AZURE_WEBAPP_NAME }}
      #     publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_SECRET }}
      #     package: ./out
      
      - name: Deploy to somee Web App 🚀
        uses: SamKirkland/FTP-Deploy-Action@4.3.0
        with:
          server: bookVue.somee.com
          port: 21
          username: eskantu
          password: Mayito.com.98
        #  log-level: verbose
          dry-run: true
          local-dir: ./out/
          server-dir: www.bookVue.somee.com/
